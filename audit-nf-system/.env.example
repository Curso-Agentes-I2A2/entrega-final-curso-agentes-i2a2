# API Keys
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# JWT Secret (gere uma chave segura)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database (opcional, já está no docker-compose)
DATABASE_URL=postgresql://audit_user:audit_password_2024@postgres:5432/audit_nf_db

# Redis (opcional, já está no docker-compose)
REDIS_URL=redis://redis:6379

# Environment
ENVIRONMENT=development

# Pytest Args (para testes)
PYTEST_ARGS=-v --cov=. --cov-report=html --cov-report=term